[package]
    name = "super-tts-registry-types"
    version.workspace = true
    authors.workspace = true
    edition.workspace = true
    description = "Canonical types for backend.toml manifests and registry.toml entries"
    license = "GPL-3.0-only"

[[bin]]
    name = "gen_schemas"
    path = "src/bin/gen_schemas.rs"
    required-features = ["schema"]

[dependencies]
    hex = "0.4.3"
    ring = "0.17.14"
    schemars = { version = "1", optional = true }
    semver = "1"
    serde.workspace = true
    serde_json.workspace = true
    spdx = { version = "0.13.4", default-features = false }
    thiserror.workspace = true
    toml.workspace = true
    utoipa = { workspace = true, optional = true }

[dev-dependencies]
    jsonschema = { version = "0.46.5", default-features = false }

[features]
    schema = ["dep:schemars"]
    # Derive `utoipa::ToSchema` on the index leaves the daemon re-exports as its
    # `/registry/backend/list` response shapes (see `super_tts_shared::registry`),
    # so they carry their own definition into the generated OpenAPI document
    # rather than being re-described there. Separate from `schema`, which is
    # schemars for the backend.toml / registry.toml JSON Schemas.
    openapi = ["dep:utoipa"]

[lints]
    workspace = true
