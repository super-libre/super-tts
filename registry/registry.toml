# SPDX-License-Identifier: GPL-3.0-only
#:schema https://jorge-menjivar.github.io/super-tts/registry.schema.json
# Super TTS backend registry. One entry per installable backend, alphabetically
# sorted. See registry/README.md for the submission rules.
#
# Empty: the catalog inherited from Super STT listed ASR backends (whisper,
# voxtral, qwen3-asr, and the deepgram/mistral/openai transcription APIs). None
# of them synthesize speech, and the repos a renamed entry would point at do not
# exist. Entries are added back as TTS backends are published.
